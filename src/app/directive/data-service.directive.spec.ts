import { DataServiceDirective } from './data-service.directive';

describe('DataServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new DataServiceDirective();
    expect(directive).toBeTruthy();
  });
});
